{
  "name": "jhs-ya",
  "version": "5.0.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build && node scripts/check-sources.mjs && node scripts/regression-check.mjs && node --check JHS.user.js && node --check dist/JHS.user.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "vitest": "^4.1.8"
  }
}
