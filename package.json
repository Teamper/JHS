{
  "name": "jhs-userscript",
  "version": "6.4.1",
  "description": "JAV Helper Suite for JavDB and JavBus",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Teamper/JHS.git"
  },
  "bugs": {
    "url": "https://github.com/Teamper/JHS/issues"
  },
  "homepage": "https://github.com/Teamper/JHS#readme",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:dev": "node scripts/build-dev.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:js": "tsc -p tsconfig.architecture.json && node scripts/checkjs-coverage.mjs",
    "check:architecture": "node scripts/architecture-check.mjs",
    "check:integrations": "node scripts/integration-quality-check.mjs",
    "check:performance": "node scripts/performance-budget-check.mjs",
    "check:release-smoke": "node scripts/manual-smoke-check.mjs",
    "check": "npm run test && npm run build && npm run check:js && npm run check:architecture && npm run check:integrations && npm run check:performance && node scripts/check-sources.mjs && node scripts/release-contract.mjs && npm run check:release-smoke && node scripts/regression-check.mjs && node scripts/contrast-audit.mjs && node scripts/ui-consistency-audit.mjs && node --check JHS.user.js && node --check dist/JHS.user.js",
    "check:browser": "npm --prefix tests/browser test",
    "check:full": "npm run check && npm run check:browser"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jquery": "^3.7.1",
    "jsdom": "^26.1.0",
    "typescript": "5.9.3",
    "vitest": "^4.1.8"
  }
}
