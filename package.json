{
  "name": "jhs-userscript",
  "version": "6.3.0",
  "description": "JAV Helper Suite for JavDB and JavBus",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Teamper/JHS.git"
  },
  "bugs": {
    "url": "https://github.com/Teamper/JHS/issues"
  },
  "homepage": "https://github.com/Teamper/JHS#readme",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build && node scripts/check-sources.mjs && node scripts/regression-check.mjs && node scripts/contrast-audit.mjs && node scripts/ui-consistency-audit.mjs && node --check JHS.user.js && node --check dist/JHS.user.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jquery": "^3.7.1",
    "jsdom": "^26.1.0",
    "vitest": "^4.1.8"
  }
}
