# JHS 6.5.0 RC parity matrix

这张表只记录当前 `refactor/6.5` 候选的可追溯证据。状态只能使用 `PASS / REGRESSION / INTENTIONAL_CHANGE / NOT_TESTED / BLOCKED`；`PASS` 表示对应证据已经在当前工作树复现，不代表真人 Smoke 已完成。

| 用户路径 | 6.4.1 基线 | 6.5 RC 预期 | 单元/静态证据 | 浏览器 fixture | 真人证据 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 全新安装 | 默认设置与空数据 | 两个截图开关、评论默认开启 | settings migration tests | — | `install.fresh` | NOT_TESTED |
| 6.4.1 → 6.5 升级 | 旧设置可读 | 设置、数据版本与活动数据保留 | migration/settings tests | — | `upgrade.v641ToV650` | NOT_TESTED |
| WebDAV 同安装备份恢复 | 固定密钥兼容 | 新备份使用 portable key，安装凭证不进入备份 | credential/backup tests | — | `backup.crossInstallRestore` | PASS |
| WebDAV 旧 6.4.1 备份 | legacy decrypt | 继续可读，导入时不覆盖目标凭证 | credential compatibility test | — | `backup.crossInstallRestore` | PASS |
| NewVideo 手动移除 | 曾写空 `car_list` 墓碑 | 只写 `dismissed` 决策，History/Stats 不增记录 | state-service tests | — | `newVideo.dismissNoPollution` | PASS |
| NewVideo 旧墓碑 | 无迁移契约 | 仅有未撤销移除活动证据才转换 | migration-v2 tests | — | `newVideo.dismissNoPollution` | PASS |
| Review 缺省 | 目录与运行时不一致 | 缺省 `yes`，显式 `no` 仍关闭 | review/settings tests | detail fixture | `review.defaultEnabled` | PASS |
| 截图开关独立 | 历史上混用 | 列表按钮与详情/FC2 自动加载分别生效 | settings/screenshot tests | detail/FC2 fixtures | `settings.screenshotIndependent` | PASS |
| 公共 HTTP 缓存 | 仅内存 | IndexedDB L2 跨刷新复用，按 TTL 失效 | cache/http tests | — | — | PASS |
| 公共缓存 prune | 写入路径同步全量扫描 | 每 64 次持久写入后台 prune，避免 O(N²) | cache complexity test | — | — | PASS |
| 公共缓存在途请求 | clear 后旧响应可能回填 | clear 递增 generation，旧响应不再写入 L1/L2 | cache/http tests | — | `cache.clearForcesNetwork` | PASS |
| session 缓存 | 仅内存 | 仍不持久化认证数据 | cache tests | — | — | PASS |
| 清除第三方/全部缓存 | 清理 legacy TTL | 同时清理公共 L1/L2；下一请求真实联网 | cache tests | — | `cache.clearForcesNetwork` | PASS |
| 冲突事务恢复 | 可能丢 journal 证据 | 先归档最多 20 条，再释放 journal | state-service tests | — | — | PASS |
| 列表刷新收敛 | 旧刷新可能吞掉最新状态 | refresh coalescing 保证最新 revision 最终提交 | list refresh tests | `setting-effects.spec.js` | — | PASS |
| JavDB/JavBus/FC2 页面 | 6.4.1 页面行为 | 保持现有路径 | existing regression suite | `check:browser` | 对应页面 key | NOT_TESTED |
| 视觉布局与真实 viewport | 6.4.1 视觉基线 | 保持现有布局 | — | `check:visual` | `mobile.realViewport` | NOT_TESTED |

## 当前 RC 边界

### 2026-09-08 深审回归修复

以下表格为上轮自动化证据。追加审计重新打开凭证清理与预览生命周期相关项，不能据此宣称 14 项已经完整收口；当前结论见下方追加修复验收。

| 用户路径 | 修复后的行为 | 自动化证据 | 状态 |
| --- | --- | --- | --- |
| 双标签同时升级凭证 | 上轮源清理存在竞态，旧密文认证失败处理不完整 | 追加凭证故障注入已复现 | REGRESSION |
| 保存 `AES:` 开头的密码 | 按明文加密并能原样读取；删除失败不能报告成功 | credential-service 明文与删除回读测试 | PASS |
| WebDAV 陈旧表单只改用户名 | 只提交修改字段，不覆盖其他标签页的密码 | reviewed-ui-regressions 表单测试 | PASS |
| 撤销失败后再次撤销 | 业务数据及撤销资格一起恢复 | state-service 六个持久化阶段故障注入 | PASS |
| 熔断恢复后遇到业务错误或取消 | 释放探测权；探测所属请求可以正常重试 | http-recovery 半开、重试与取消测试 | PASS |
| 修改网络设置后继续请求 | 下一逻辑请求使用新设置；读取请求保持总尝试次数语义 | http-recovery 3/10 次尝试与变更请求测试 | PASS |
| 请求未结束时清缓存再请求 | 新代次独立联网；旧请求不回填、不删除新请求 | http-recovery / http-service 缓存测试 | PASS |
| 请求准备期间关闭所属界面 | 不遗留消费者或失去所有者的底层请求 | http-recovery / http-service scope 测试 | PASS |
| 旧评论关键词及新增关键词 | 使用同一 portable 键，合并误写旧键且保留失败来源 | review-live-regressions 合并、并发及失败测试 | PASS |
| 评论初始关闭后开启、再次关闭 | 两站可实时挂载；关闭后禁止分页并丢弃旧响应 | review-live-regressions；reviewed-regressions 桌面/移动端 fixture | PASS |
| JavBus 关闭标题翻译 | 恢复原题并保留宿主链接 | reviewed-ui-regressions / translate-dom-contract | PASS |
| 关闭 DMM 增强后使用原生预览 | 上轮提前挂载工具栏，遗漏开关与关闭竞态 | 追加浏览器行为测试已复现 | REGRESSION |
| 批任务初始化失败后重试 | 列表与黑名单都释放任务标记并恢复按钮 | reviewed-ui-regressions 批任务故障测试 | PASS |
| 数字搜索从第三页执行全页操作 | 正确保留数字条件并解析第一页地址 | reviewed-ui-regressions / batch-scope / batch-scanner | PASS |

凭证内部格式已升级为 schema 2；兼容读取旧格式及旧 portable 备份，手动降级旧脚本可能需要重新填写凭证。引导增加一次微任务边界以减少首次资源请求的同步开销，资源顺序与 ready 条件保持不变，启动预算未调整。

### 上轮机器验收（历史记录）

| 门禁 | 2026-09-08 当前工作树结果 |
| --- | --- |
| `npm run check` | PASS：161 个测试文件、986 项测试；类型、架构、集成、产物体积、语法、发布契约和 UI 审计全部通过 |
| `npm run check:browser` | PASS：97 passed、317 skipped；跳过项来自项目及 viewport 分工，不能作为已执行证据 |
| 启动性能，Edge desktop-wide，各 30 样本 | PASS：JavDB P50 218.2 ms / P95 233.3 ms，上限 242 ms；JavBus P50 225.2 ms / P95 239.5 ms，上限 253 ms |
| `npm run check:visual` | PASS：27 passed、1 skipped，未更新视觉基线 |
| `codegraph sync` / `git diff --check` | PASS |
| 构建产物 | 根目录与 dist 字节一致，1,007,740 bytes；SHA-256 `81a73407b7b2a73db93a0b314af92966a8a91d6189e44d9548a10db535884c2f` |
| `npm run check:release-smoke` | BLOCKED：`testedAt must be an ISO date-time`；真人记录仍为占位，未填写或伪造验收结果 |

性能诊断使用相同 fixture 对照了 `526b168` 和固定基线 `4ea5536`，两者在本次环境均超预算；逐段计时将稳定热点定位到首次外部样式插入（约 29–33 ms）。增加微任务边界后，原门禁中的 `pre-settings` 阶段约为 2 ms，完整计时终点及预算保持原样。

- 版本保持 `6.5.0`，不创建 `6.5.0-rc.1` 标签。
- `docs/release/manual-smoke-v6.5.0.json` 仍是明确失败的占位记录，不能替代 Edge/Chrome + Tampermonkey 真人证据。
- 最终候选必须在冻结后重新计算 `JHS.user.js` SHA-256，并把同一哈希写入真人记录。


### 2026-09-09 追加修复验收

按追加审计修复 A01–A12，并实现 A13 的局部三列布局。A13 只有宿主 CSS 与历史结构的 fixture 证据；真实详情 URL 仍跳转到登录页，不能称为现场已验证。

| 用户路径 | 当前实现与验证边界 | 状态 |
| --- | --- | --- |
| 旧凭证升级、冲突及清空 | GM 单条 schema 2 记录；保留页面旧源和密钥；已标记/疑似密文无法认证时不回退明文；删除记录阻止重新导入 | PASS（自动化） |
| 离线连点、取消、关闭、失败重试 | 首个异步边界前取得操作标记；云端成功、本地历史失败、广播失败分别处理；不自动重试外部提交 | PASS（自动化） |
| 列表 → JavDB iframe → 原生/DMM 预览 | 未打开时无预览工具栏；关闭清理、重开单实例；当前播放器快进；无原生视频使用真实独立播放容器 | PASS（自动化） |
| 本页及跨标签状态 | 主操作区和已打开预览同步收藏、屏蔽状态；状态事件先使影片缓存失效 | PASS（自动化） |
| 磁力排序、过滤与宽窄布局 | 360/390/768/992/1248/1408 CSS px；保留宿主节点及事件；浅深主题条件截图（不证明真实宿主配色）；CSS 缩放 100%/125%/150% 条件验证不等同浏览器原生缩放 | PASS（自动化） |
| JavDB 登录后磁力现场 | 无已登录浏览器会话；尚未取得现场 DOM、计算样式与实际缩放证据 | BLOCKED |
| Tampermonkey 实机验收 | 当前清单 24 项，包含 115/123 的真实用户操作；占位记录不作为证据 | BLOCKED |

版本保持 6.5.0，不提交、推送或发布。凭证、旧源、密钥及删除记录不进入 portable 备份或诊断。前述历史机器结果及旧哈希不能代替当前产物验收。

### 本轮机器验收结果

| 门禁 | 2026-09-09 结果 |
| --- | --- |
| npm run check | PASS：161 个测试文件、1000 项测试，构建及静态门禁通过 |
| npm run check:browser | PASS：122 passed、364 skipped；跳过项不作为执行证据 |
| 启动性能，各 30 样本 | PASS：JavDB P50 205.5 ms / P95 217.2 ms；JavBus P50 210.2 ms / P95 231.8 ms；上限仍为 242 / 253 ms |
| npm run check:visual | PASS：27 passed、1 skipped，未更新基线 |
| codegraph sync / git diff --check | PASS |
| 构建产物 | 根目录与 dist 字节一致，1,015,513 bytes；SHA-256 30facdd7328c007913d20a039540a8fff0be3191b14ca147dcc4c73253fce998 |
| npm run check:release-smoke | BLOCKED：testedAt must be an ISO date-time；24 项真人记录仍是占位 |

自动化使用 mock 离线 provider，未创建真实云端任务。真实 JavDB 登录详情、浏览器原生缩放、宿主实际主题及 Tampermonkey 验收证据尚缺，RC 继续保持发布阻断。

### Chrome 实测后的追加修复（2026-09-09）

用户更新上一候选后，真实登录页面已验证磁力桌面三列、过滤 7→1→7 行和窄屏 348px 容器无溢出；窄屏操作按钮为 44px。此前登录页面完全不可用的记录已被这些有限证据更新，仍不能代替完整缩放、主题和人工验收。

实测发现详情弹窗在视口收窄后仍维持旧宽度，以及 DMM 未加载时隐藏暂停的原生播放器。本轮新增回归测试在修复前均失败。现在弹窗按 resize 重算尺寸和 iframe 高度，关闭时释放 LifecycleScope；DMM 等待可播放状态，错误或 10 秒超时保留原生画面及暂停状态，关闭会话取消等待，画质切换也检查加载结果。

当前源码已通过 162 个测试文件、1004 项测试及静态门禁；完整浏览器门禁 126 passed、372 skipped；视觉门禁 27 passed、1 skipped，未更新基线。启动各 30 样本：JavDB P50 209.1ms / P95 217.9ms；JavBus P50 211.1ms / P95 217.6ms，仍满足 242/253ms 上限。CodeGraph 与 diff 检查通过。新产物 SHA-256：a34ee5d8c7ddadf327024049d9fbb685387ec440b582b22c3efe565074ae6f69，根目录与 dist 均为 1,016,918 bytes。用户已更新 Chrome 脚本，实机复验确认弹窗随视口缩放：390px 视口中 iframe 宽 372.4px、左侧 8.8px；恢复 1248px 视口后 iframe 宽 1214.4px。开启 DMM 后两次打开预览均未观察到 DMM 接管，原生播放器仍可见；DMM 成功接管及 10 秒超时现场路径仍为 NOT_TESTED。复验后恢复原设置与视口。未执行真实离线或下载任务，不填写真人 smoke 记录。


### 全界面 UI 整改（2026-09-09，工作区候选）

功能基线已分 14 个本地提交保存，范围 aa7826f..e6eacef；未推送。UI 候选从 e6eacef 开始，保持 6.5.0，尚未提交。

规范归属：theme/ui-primitives 管理 36/32/44px 控件、8px 圆角及交互反馈；state-actions 只构建状态控件，DetailStateController 保持行为；core/dialog-shell 消费内部 ui.size/ui.body，保留第三方关闭策略与回调；createJhsTable 标记自有表格，行操作使用紧凑尺寸。未修改存储、凭证和服务协议。

以下清单依据插件注册、渲染入口和弹窗调用核对。代码接入不等于实站验收；fixture 不能替代登录站点与真人 Tampermonkey。

| 界面/入口 | 差异与整改归属 | 证据边界 |
| --- | --- | --- |
| JavDB/JavBus 列表、卡片状态、批处理、排序与分页 | 复用基础控件；紧凑分段保留 32px/6px；触摸热区 44px | 原浏览器行为回归；新候选 JavDB 普通卡片实测，其他路径不扩大结论 |
| 普通详情四状态组 | 移除 120px 固定宽；统一 min 80px；容器不足 480px 两列 | 新六宽度/浅深主题计算几何测试 |
| FC2 独立详情及列表弹窗 | 共用状态组；两列组独占整行，其他操作自适应 | 原 FC2 路径与视觉回归 |
| JavDB 原生/DMM 预览 | 状态子集复用，快进次级色；预览打开时隐藏背景 FAB，关闭恢复 | 原生快进、迟到回调、跨标签、开关回归；真实 DMM 接管/超时待验证 |
| JavBus 预览、截图和 Viewer/Fancybox | 控件令牌沿用；保留 vendor 自身播放/关闭机制 | fixture；实际媒体网络未验证 |
| JavDB 原生磁力 | 保留节点/事件，复制、下载、离线统一次级外观 | 三列/窄屏/排序过滤、按钮计算样式；旧候选已有实站三列证据 |
| 第三方磁力来源、磁力搜索 | 原有工具条与基元；弹窗外壳集中限宽高 | 原解析、磁力与开关回归 |
| 字幕查询、下载结果表 | 复用表格行控件与弹窗外壳；不触发真实下载 | fixture/mock |
| 评论、相关清单 | 原加载/空态/重试基元，补齐自有深色背景 | 原生命周期及开关测试 |
| 设置：备份、基础、屏蔽、定时、外部站点、缓存 | 设置渲染器与基础控件；标签扩大开关热区 | 可见分类在 390/1248px 无横向溢出；不点击数据动作 |
| 设置：体检、插件、恢复点、外部请求、资源来源、云盘、数据工具 | 同上；规则编辑与磁力源编辑使用公共外壳 | 设置分类及既有 mock；不进行真实恢复/删除/云盘提交 |
| 备份列表、导入预览与冲突、恢复确认 | 公共外壳/表格控件；确认策略不变 | 原单元/fixture；真实恢复禁止执行 |
| 鉴定记录：状态、活动、离线记录及行菜单 | createJhsTable 紧凑 32px；历史纵向布局，分页在内部 | 新分页几何回归及真实 Chrome 状态表窄屏翻页；活动/离线仅自动化 |
| 演员黑名单、任务状态 | 表格 body 模式；保留筛选/分页布局 | 原批处理及浏览器回归 |
| 新作品工作区、演员编辑、头像与 CDN 选择 | 公共外壳/尺寸预设；保留工作区自身布局 | 原新作品测试及 fixture |
| 统计、诊断、日志/源码查看 | 复用外壳、基础表单及代码查看器 | 自动化范围内；实际数据量待实站验证 |
| 识图入口与结果、演员信息/收藏、Top250 | 基础控件及弹窗边界；保留图像/列表密度 | 原路由、媒体与 Top250 回归 |
| 115 匹配、登录提示、重命名入口及服务选择 | 现有 jhs-btn 继承统一规则；服务选择复用外壳 | mock；不进入真实数据操作 |
| 123 授权提示 | 现有通知组件；该站插件没有独立表单或工具栏 | 静态入口核对与 mock；不访问凭证 |
| JavTrailers、SubtitleCat | 保留原生媒体/站点控制；JHS 消息继承通知组件 | 静态入口及既有 fixture，实站未验证 |
| 通用确认/警告、图片查看、toast、FAB | 关闭键盘语义、resize 与监听清理；图片 vendor 不另加控件 | 外壳单元与既有交互；真人 smoke 待执行 |

新增用例对 e6eacef 同产物的反证：状态几何与设置缩窄两项均失败；当前实现通过。设置基线中未出现标题栏属于原入口配置，不补造新标题。视觉截图中的宿主原生白底不证明宿主深色适配；JHS 自有内容单独使用主题背景。

用户已安装本轮 UI 候选，真实 Chrome 登录 JavDB 已核对列表点击进入 iframe 详情：关闭预览时只有一组四状态按钮，桌面均为 80×36px；390px 视口下弹窗宽 374px，按钮两列且高度 44px，无横向溢出。磁力三个操作同高、同行、同色和圆角；设置弹窗缩窄后保存按钮仍可见，云盘分类仅查看布局。原生预览打开后才出现两状态与快进工具栏，实际快进使播放位置前进；详情关闭后会话销毁。未执行离线、下载、删除或恢复。

真实历史弹窗检查发现分页底部超过弹窗边界：旧布局类未接入已有纵向 flex 规则。已改用历史布局类并移除表格旧固定高度类，新增几何测试在修复前失败、修复后通过（1408/390/768px）。用户再次安装后已刷新 Chrome，现场 DOM 确认为 jhs-history-layout / display:flex；宽屏分页底部 1046.9px 小于弹窗底部 1073.5px。390px 视口中分页底部 809.4px 小于弹窗底部 836px，点击下一页后活动页码为 2，随后恢复原视口并关闭弹窗。

原生 125%/150% 浏览器缩放、全界面实测及 DMM 成功接管/超时路径保持 NOT_TESTED；视口调整不等于原生缩放。真人 smoke 记录不修改，RC 仍 BLOCKED。

#### UI 候选性能复核

最终追加历史布局修复后的全量复测再次超过原预算，不能沿用前一轮通过结论。加入可选 JHS_STARTUP_BUNDLE 仅用于读取指定对照产物，默认仍读取根目录脚本，计时起止、诊断、请求和 30 样本标准均未改变。同一 Edge fixture 对照 UI 修改前的 e6eacef 同产物，结果如下：

| 构建 | JavDB P50 / P95 | JavBus P50 / P95 | 结论 |
| --- | --- | --- | --- |
| UI 修改前产物 | 272.5 / 299.0 ms | 276.7 / 295.5 ms | 超过 242 / 253 ms |
| 最终 UI 候选 | 272.7 / 283.9 ms | 264.1 / 277.0 ms | 超过 242 / 253 ms |

两版都超预算，阶段数据同时出现 harness 与引导阶段变慢，尚不足以归因于 UI 修改，也不能证明环境原因已排除。未调整预算或提前 ready，性能门禁保留 FAIL，待稳定环境复核与进一步阶段对照。日志：output/ui-performance-baseline-final.log、output/ui-browser-final3.log。设置 resize 用例改为等待相同几何条件成立，避免在浏览器发出 resize 事件前读取旧尺寸；未放宽尺寸断言。

#### 最终候选门禁汇总

| 门禁 | 当前结果 |
| --- | --- |
| npm run check | PASS：163 文件、1005 测试，构建及静态检查通过 |
| npm run check:browser | FAIL：128 passed、392 skipped、2 failed；失败仅为两站启动性能，不能标记全门禁通过 |
| npm run check:visual | PASS：27 passed、1 skipped；13 张 UI 预期变化基线经逐类审查更新，未批量接受其他差异 |
| codegraph sync / git diff --check | PASS |
| 根目录 / dist 产物 | 字节一致，1,022,192 bytes；SHA-256 93fb147f8fdc07cfb028bebb505b6cb2f006d1f698fc0a451da5b6181602e864 |
| npm run check:release-smoke | BLOCKED：testedAt must be an ISO date-time，真人记录仍未完成 |

UI 整改代码保留在工作区，未提交、推送或发布；当前本地 HEAD 仍为 e6eacef。功能与有限 Chrome 路径证据已补齐，性能及上述实机缺口未收口，不能标记全部整改验收完成或 RC 就绪。

### 用户反馈后的离线回归修复

用户报告 123 云盘提示“提取番号信息失败”。代码对照确认 526b168 将列表解析移到原生详情之前，而最近 `.item` 在 JavDB 实际属于磁力行，并非影片卡片。解析因此在调用云盘 provider 前抛错，同一入口下 115 也受影响。现限定影片列表容器中的卡片，保留显式/自有上下文优先级。此前离线 mock 直接提供上下文，未覆盖这条原生按钮路径，不能证明它正常。

新增浏览器回归从列表点击进入 iframe，再点击真实注入的磁力按钮；云盘 provider 完全 mock，不发送云端任务。修复前 JavDB 直接详情和 iframe 在 123/115 四条路径均失败；修复后两站详情与 iframe 共 12 项桌面/移动测试通过。JavBus 使用符合宿主适配器的 table/tr/td 条件 fixture，不把旧简化 div fixture 的缺少按钮当成站点故障。

同时发现 115 在响应缺少明确成功状态时误报创建成功；五种异常响应测试修复前失败，现仅接受明确成功状态，原错误分类与无自动重试策略不变。两家云盘接口当前在线可用性仍未经真实提交确认，不能将 mock 通过扩大为真实服务端验收。

最终实现复用 HostAdapter.locateListItems 判定所属卡片，避免把站点选择器再次写入业务插件。npm run check：163 文件、1010 测试全部通过；离线新增浏览器路径、列表卡片归属及既有详情审计回归共 46 项通过；codegraph sync 和 git diff --check 通过。根目录/dist 均为 1,022,386 bytes，SHA-256 a7d42cfd3efb4e82253c52015faff96b17f727041609f9b654e350a07978187a。该产物替代上方 UI 候选哈希，尚未由用户更新实机；本次未重跑完整性能/视觉门禁，不撤销既有性能与真人 smoke 阻断。未创建任何真实离线任务，未提交代码。

### 持续真实路径审计：新作品窗口

刷新真实 Chrome 后，在 390×844 视口打开已有数千条记录的新作品窗口：列表容器仅 89px 高，分页 top=21371.7px，位于 60 张卡片末尾，不能直接翻页。已将自有分页放到卡片滚动区之外、批操作区之前；切换视图、重绘和空态时清理或隐藏旧分页。窄屏任务状态保留全部信息，改为横向滚动，避免三行状态挤占列表高度。

75 条 mock 记录回归在修复前因分页不在视口失败；修复后 1248/390/360px 视口中分页完整可见、列表至少 150px，下一页显示剩余 15 条，切换演员视图后新作品分页隐藏。该局部修复等待用户安装后的真实数据复验。

补查 360×640 矮窗口时列表曾被压到 43px，现保留 160px 最小高度并允许工作区滚动，分页和顶部工具仍可到达；刷新加载期间清理旧分页，防止点击旧缓存页干扰新渲染。对应几何及延迟加载交互回归通过。

本轮真实只读检查还覆盖黑名单桌面及窄屏（表格内容 316px 高，分页在窗口内，无横向溢出）、列表打开 FC2 详情（成功打开自有工作区，四状态按钮两列/44px，无横向溢出）。这些路径未复现新故障，不代表全部功能通过。未点击真实离线、下载、删除、同步检测或恢复操作。

本轮最终检查：npm run check 全部通过（163 文件、1010 测试）；新作品/离线/共享 UI 相关浏览器回归 21 passed、5 skipped；视觉 27 passed、1 skipped，未新增更新视觉基线；codegraph sync 与 git diff --check 通过。根目录/dist 一致，1,022,816 bytes，SHA-256 5a2b7de4e1d811dbecafb006a63ddaac3ebad559e9ec8af457423e76779fa5ac。未重跑完整 30 样本性能，不撤销前述性能与真人 smoke 阻断。代码未提交，等待更新后真实数据复验。

### 继续实测：嵌套表单、搜索及新作品计数

刷新 Chrome 后，现场确认已安装新作品布局修复（分页父节点为 newVideoToolBox）。5472 条数据翻到第 2/92 页成功；390×844 视口中列表高 160px，分页 top=686px、bottom=774.5px；360×640 中列表仍高 160px，外层可滚动。无匹配搜索移除卡片及分页，清除搜索后恢复 60 张卡片和第 1 页。这些证据补全前一节的真实数据复验。

设置→资源来源→新建标签规则，Escape 只关闭子窗口，父窗口保留。数据工具的解析预览在修改输入后禁用确认，未执行导入。以上路径未发现新的功能故障，临时表单已关闭，视口已恢复。

另经代码对照和数据测试确认新作品计数漏算：getPendingNewVideoTotal 以 carMap.has 排除记录，但工作区以四个状态是否全空决定待处理，取消全部状态的记录会造成“工作区 1 条、计数 0 条”。已统一状态判断；新增测试先失败后通过，同时验证取消另一条收藏后计数恢复为 2。此问题以 fixture 数据复现，未修改真实记录，也不声称现场已触发该条件。

计数修复后的 npm run check 通过（163 文件、1011 测试），新作品及共享 UI 浏览器回归 5 项通过；codegraph sync、git diff --check 通过。根目录/dist 一致，1,022,835 bytes，SHA-256 baf8a389c423872e58e266cc68860f84628b22312cdb5a619a42200371ab2fa8。未重跑全量性能/视觉门禁，既有性能与真人 smoke 阻断继续保留；该计数修复尚待用户更新安装，源码未提交。

### 无可用离线服务提示

用户报告“没有已启用且支持该资源的离线服务”。真实 Chrome 设置显示 123 已启用、115 未启用，123 插件也启用；123 云盘页面仍登录，已刷新该页让现有脚本重新运行授权同步。当前详情按钮的 DOM 资源为 Magnet，属于 123 支持格式。未读取或输出 token，未点击离线提交，授权是否同步及详情服务是否加载仍待确认。

代码确认不可用结果缓存 20 秒，且统一错误隐藏了每个服务被排除的原因。现手动提交无候选时强制复查一次可用性，保留成功缓存和生命周期检查；错误提示明确显示未启用、不支持的格式、授权不可用。新增两项测试在修复前失败，修复后通过。该修复不能替代真实授权是否同步或云端接口是否正常的证据。

另补充 123 授权桥接插件未加载或已禁用的明确提示。最终 npm run check 通过（163 文件、1013 测试），相关浏览器回归 46 项通过（mock provider）；codegraph sync、git diff --check 通过。根目录/dist 一致，1,023,493 bytes，SHA-256 8d8f17a87aee97fb5cf3bbab5b344baaee0f96298c4b74c3a912943b15157d0b。未执行真实云端提交，未提交代码；完整启动性能和人工 smoke 的既有阻断继续保留。

### 详情面板视觉割裂：2026-09-09

在用户截图对应的 MD0325 列表→详情 iframe 中，实测磁力区继承宿主灰底及 message-body 的 20px/24px 内边距；评论和相关清单插槽横向内边距为 0，内容与折叠按钮贴边，标题为 18px/400。基于现场结构补充脱敏 fixture，避免继续只使用没有宿主包装层的历史详情 fixture。

宿主适配边界只增加磁力面板标记；布局样式清理资源区 columns/column/message-body 叠加留白，使用与自有面板一致的底色、边框、12px 圆角和 16px 内边距。评论与清单共用 16px/600 标题、行间距和分隔线，保留折叠/重试/加载更多及原链接，长标题换行。未移动或重建宿主磁力节点，未改业务数据格式。

新增真实点击列表→iframe 的浏览器回归在原产物失败，修复后覆盖 360/390/768/992/1248/1408px、浅深主题、长文本、面板几何、折叠恢复及宿主节点保留。相关浏览器回归合计 55 passed、125 skipped（测试归属限制）；npm run check 为 163 文件、1013 测试通过。视觉逐张审查后只更新 6 张普通详情基线；另发现 FC2 深色截图在启动完成前切换主题，补充 first-ready 等待及主题属性断言，未接受异常浅色截图。最终视觉 27 passed、1 skipped。

用户安装后已刷新 Chrome 并重新从列表打开 MD0325，现场确认三个面板 left=16px、width=1400px，内边距 16px（含边框的内容偏移 17px）、圆角 12px、白色底、横向溢出 0，评论/清单标题 16px/600。后续折叠点击时 Chrome 控制连接中断，因此修复后的真人折叠、窄屏和深色截图不记为现场通过；这些交互和主题范围仅有 fixture 证据。

根目录/dist 一致，1,025,887 bytes，SHA-256 9fd6b7efff2a54831b1c13196febd1883b6cf2cd0d16f887f6270cf7425cb0c5。未执行真实离线或下载，未提交代码；本轮未重跑 30 样本启动性能，既有性能和人工 smoke 阻断仍保留。


### 发布前五项缺陷修复：2026-09-09

本轮替代受五项缺陷影响的发布就绪判断；历史实测记录只证明其对应产物和操作范围。五个新增浏览器故障用例在修复前全部失败，123 凭证的错误密钥、缺失密钥、损坏密文三项同样失败。修复未改变版本、公共服务接口或备份协议，未提交或发布。

| 缺陷 | 修复与行为证据 | 实站证据 |
|---|---|---|
| 确认框 HTML 注入 | 转义评论、标题、关键词、演员/分类、来源规则及影片确认文案的动态数据；固定 Layer 1.0.9 实际渲染，危险内容保留为文字且确认后保存原文 | 不在真实站点注入攻击样例；隔离 fixture 验证 |
| 云盘开关关闭仍生效 | 云盘布尔写入规范化，兼容旧值；候选及提交前读取最新快照；115 匹配独立 scope、启停与迟到结果保护 | 新候选待用户安装后刷新复验；无真实云端操作 |
| 123 密文被当作令牌 | 标记密文严格解密，失败返回不可用且保留源；旧明文继续可读，重新同步恢复，AES: 开头新来源值也加密 | 实际授权/提交仍为真人 smoke 范围 |
| 115 匹配徽标误触导航 | 失败重试、空/单/多结果均阻止父卡片导航；fixture 验证页面地址、播放目标及选择弹窗 | 使用 mock，不访问用户云盘文件 |
| 相关清单跨标签不同步 | 双标签同步展开状态，首次加载取消后重开、分页失败重试；多个 owned 面板独立清理请求和订阅，FC2 接入 ownCleanup | 新候选待安装；此前普通详情和 FC2 检查不覆盖此修复 |

专项浏览器 11 项通过；npm run check：165 文件、1028 测试通过，类型、架构、集成、请求预算、对比度、UI 和产物检查通过。完整 npm run check:browser：159 passed、489 skipped（既有项目归属限制）；npm run check:visual：27 passed、1 skipped，本轮未修改视觉基线。JavDB/JavBus 各 30 样本，P50 分别为 200.1/201.8ms，P95 为 218.1/227.9ms，通过原 242/253ms 上限，计时边界和请求预算未调整。codegraph sync、git diff --check 通过。

根目录/dist 产物一致，1,029,315 bytes，SHA-256 e00a3ad4529efc063eaaec2f10fb7bb6f297ca6c0c41e8dcf07ad87c84d5ed6f。新候选已通知用户安装，尚未取得安装回复及刷新后的 Chrome 实测证据。check:release-smoke 未通过（testedAt must be an ISO date-time），现有记录不能证明当前产物完成真人验收。真实离线、下载、删除、恢复均未执行；DMM 成功接管/超时和真人 smoke 缺口继续独立阻断发布，不标记 RC 就绪。

### 用户安装后继续审计：2026-09-09

用户确认更新后重新加载 Chrome 页面。普通列表→详情 iframe、FC2 五分区、相关清单跨标签及普通详情→FC2 展开同步、清单分页 20→40（40 个唯一链接）、原生预览快进及关闭清理、鉴定记录翻页、Top250 可见卡片详情均得到实站证据。123 开关关闭后重开仍关闭，已恢复用户原有开启状态；相关清单恢复原有展开状态。115 离线与匹配保持关闭，没有真实云端操作。浏览器安装字节未独立读取，本地哈希不等同于扩展内字节校验。

本轮真实模块故障注入另确认两项遗漏：123 令牌读取等待期间关闭服务，令牌返回后仍提交（期望 mock 调用 0，实际 1）；旧授权同步晚于新同步完成时覆盖新令牌。后者为现存并发缺陷，未证明由最近修复引入。两项均未修改源码，需补修及正式回归用例，不能沿用上一轮机器通过结论宣布无缺陷。详细复现位于 output/post-install-audit/probe.mjs 与同目录审计结果.md。DMM 和真人 smoke 发布阻断继续保留。

### 123 异步边界补修：2026-09-09

修复上述两项：令牌读取后立即重检最新开关、scope 和操作按钮所属界面，取消不写入云端失败记录；同步写入前核对代次、来源令牌、来源类型、当前目标及密钥，旧结果失效，清空操作也终止先前同步代次。保持既有存储键、密文格式和版本，无真实云端操作。

正式回归在修复前 5 failed、8 passed，修复后补充来源变化、目标被另一写入方更新、密钥变化、清空、取消不写历史及成功仅提交一次。原审计探针重跑为提交次数 0、最新授权保留。npm run check 为 166 文件、1037 测试通过。完整浏览器为 160 passed、494 skipped（既有项目归属限制）；视觉为 27 passed、1 skipped，未变更基线。两站各 30 样本，JavDB P50/P95 为 207.6/218.3ms，JavBus 为 212.7/231.1ms，通过原 242/253ms 中位数上限。codegraph sync、git diff --check 通过。

用户确认安装后重新加载 Chrome，普通 EBWH-348 iframe 与 FC2-4963641 弹窗正常，各一组状态按钮，评论/清单及 FC2 五分区完整，无横向溢出；云盘设置水合完成，123 保持开启、115 三项保持关闭，未修改偏好。关键异步故障使用隔离 mock 验证，没有真实提交。根目录/dist 均为 1,030,041 bytes，SHA-256 ae4a7b7dfd034f4fbba2129c4d38505186780fe507969427c006a3cc010b3ea6；未独立读取扩展内脚本字节。check:release-smoke 仍因 testedAt 无效失败，既有真人 smoke 和 DMM 缺口不解除，不标记发布就绪。未提交、推送或发布。

### Escape 分层关闭补修：2026-09-09

修复设置下拉 Escape 冒泡关闭父设置，以及 iframe Fancybox 预览 Escape 连带关闭外层详情。下拉消费事件并恢复触发按钮焦点；全局处理尊重已取消事件，避让可见或正在关闭的预览，并忽略隐藏预览及归属较低 Layer 的预览。保留宿主播放器关闭逻辑，未新增第二套关闭控件。

修复前实际 Layer 用例预期父弹窗 1、实际 0，另三项事件/预览用例失败。正式回归验证已处理事件、iframe 预览打开/关闭中、隐藏预览、较低弹层预览；真实 Layer 验证两次 Escape 顺序与焦点恢复。npm run check：167 文件、1042 测试通过。完整浏览器为 160 passed、499 skipped、1 failed，唯一失败为 JavBus 启动性能；视觉为 27 passed、1 skipped，本轮未更新视觉基线。codegraph sync、git diff --check 通过。

两站各 30 样本：JavDB P50/P95 为 234.4/254.7ms，通过 242ms 上限；JavBus 为 254.8/295.7ms，超过 253ms 上限。保留 output/escape-browser.log 原始失败记录，未调整预算、计时边界或采样数量。阶段记录显示依赖注入及 fixture 等多个阶段也较上轮增长，但不足以确认环境波动或产品回归的归因；当前性能门禁仍为失败，不能标记发布就绪。

用户确认安装后刷新真实 Chrome JavDB 页面，设置下拉第一次 Escape 后父 Layer 保留、菜单收起、触发按钮获得焦点且 aria-expanded=false；第二次关闭设置。列表进入 EBWH-348 详情 iframe 并打开原生预览后，第一次 Escape 移除 Fancybox、视频暂停且隐藏，详情 Layer 保留；第二次关闭详情。未执行真实离线、下载、删除或恢复。根目录/dist 一致，1,030,307 bytes，SHA-256 99af39342eb6f59282f0c1f0f5504632cb48e51896969085d3c9edc0d117e914；浏览器安装字节未独立读取。DMM 接管与真人 smoke 发布阻断独立保留，既有人工记录不能代替当前产物验收。未提交、推送或发布。
