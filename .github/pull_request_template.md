## 改动说明

- 解决的问题：
- 采用的方案：
- 兼容性影响：

## 验证

- [ ] `npm run check`
- [ ] 未改变 `JAV-JHS`、`jhs_*`、`jhs-*` 等兼容标识
- [ ] 未直接拼接不可信外部数据到 `innerHTML` / `.html()`
- [ ] 版本变更已同步 package、UserScript metadata 和 CHANGELOG
